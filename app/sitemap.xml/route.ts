import * as data from '../data'; import {fleetServices,postsPerPage} from '../fleet-data'; import {getContent} from '../../lib/content';
export function GET(){const d=data as any,base=`https://${d.site.domain.toLowerCase()}`,blogs=d.blogPosts||[],pages=Math.max(1,Math.ceil(blogs.length/postsPerPage)),research=getContent('research'),alternatives=getContent('alternatives');const paths=['','/services','/blog','/blog/top-25-bookkeeping-finance-outsourcing-companies','/research','/alternatives','/contact-us','/privacy','/terms','/cancellation-policy',...fleetServices.map(s=>`/services/${s.slug}`),...blogs.map((b:any)=>`/blog/${b.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`),...research.map(r=>`/research/${r.slug}`),...alternatives.map(a=>`/alternatives/${a.slug}`)];const body=paths.map(path=>`<url><loc>${base}${path}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'content-type':'application/xml'}})}
