import * as data from '../data'; import {fleetServices,postsPerPage} from '../fleet-data'; import {getContent} from '../../lib/content';
export function GET(){const d=data as any,base=`https://${d.site.domain.toLowerCase()}`,blogs=d.blogPosts||[],pages=Math.max(1,Math.ceil(blogs.length/postsPerPage)),research=getContent('research'),alternatives=getContent('alternatives');const paths=['','/services','/blog','/blog/bookkeeping-outsourcing-due-diligence-checklist','/research','/alternatives','/contact-us','/privacy','/terms','/cancellation-policy',...fleetServices.map(s=>`/services/${s.slug}`),...blogs.map((b:any)=>`/blog/${b.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`),...research.map(p=>`/research/${p.slug}`),...alternatives.map(p=>`/alternatives/${p.slug}`)];const body=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${paths.map(p=>`  <url><loc>${base}${p}</loc></url>`).join('\n')}\n</urlset>`;return new Response(body,{headers:{'content-type':'application/xml; charset=utf-8'}})}
