import {notFound} from 'next/navigation'; import {Header,Footer,CTA,JsonLd} from '../../components'; import {fleetServices} from '../../fleet-data'; import {site} from '../../data';
const base=`https://${site.domain.toLowerCase()}`; export function generateStaticParams(){return fleetServices.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)return {title:'Not found'};return {title:s.title,description:s.desc,alternates:{canonical:`/services/${slug}`}}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=fleetServices.find(x=>x.slug===slug);if(!s)notFound();const url=`${base}/services/${s.slug}`;return <><Header/><main><JsonLd data={{'@context':'https://schema.org','@type':'Service',name:s.title,description:s.desc,url,areaServed:'Worldwide',provider:{'@type':'Organization',name:site.brand}}}/><section className="fleet-hero variant-1"><div className="container"><p className="eyebrow">Philippines-based bookkeeping support</p><h1>{s.title}</h1><p className="lead">{s.desc}</p><a className="btn primary" href="/contact">Discuss this service</a></div></section><section className="section"><div className="container fleet-detail-grid"><div className="card"><h2>Work to include</h2><ul>{s.tasks.map(x=><li key={x}>{x}</li>)}</ul></div><div className="card"><h2>Controls to retain</h2><ul>{s.controls.map(x=><li key={x}>{x}</li>)}</ul></div><div className="card"><h2>First-week plan</h2><ul>{s.firstWeek.map(x=><li key={x}>{x}</li>)}</ul></div></div></section><CTA/></main><Footer/></>}
