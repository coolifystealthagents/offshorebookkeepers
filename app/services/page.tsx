import {Header,Footer,CTA} from '../components'; import {fleetServices} from '../fleet-data'; import {site} from '../data';
export const metadata={title:`Services | ${site.brand}`,description:`Explore Philippines-based ${site.primary} services.`};
export default function Services(){return <><Header/><main><section className="fleet-hero variant-1"><div className="container"><p className="eyebrow">Philippines-based services</p><h1>Offshore Bookkeepers services</h1><p className="lead">Choose a focused bookkeeping workflow, then define its tools, schedule, approvals, and handoffs.</p></div></section><section className="section"><div className="container fleet-service-grid">{fleetServices.map((s,i)=><a className="card fleet-service-card" href={`/services/${s.slug}`} key={s.slug}><span>{String(i+1).padStart(2,'0')}</span><h2>{s.title}</h2><p>{s.desc}</p><b>View service →</b></a>)}</div></section><CTA/></main><Footer/></>}
